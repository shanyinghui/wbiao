server:
  port: 9000
spring:
  application:
    name: wbiao-gateway

eureka:
  client:
    registry-fetch-interval-seconds: 5  #表示eureka client间隔多久去拉取服务注册信息
    service-url:
      defaultZone: http://127.0.0.1:7001/eureka/

  instance: #服务名称的修改
    instance-id: wibao-gateway-zuul
    prefer-ip-address: true     #访问路径可以显示IP地址



#zuul网关的配置
zuul:
  prefix: /api # 设置统一的公共前缀
  ignored-services: "*"
  routes:
    server-gooignored-servicesds: #面向服务的路由配置，可以自定义设置名称
      serviceId: wbiao-goods #服务名
      path: /item/** #映射的url

info:
  app.name: wbiao-gateway
  company.name: www.wbiao.com
  build.artifactId: ${project.artifactId}
  build.version: ${project.version}
  build.description: ${project.description}
