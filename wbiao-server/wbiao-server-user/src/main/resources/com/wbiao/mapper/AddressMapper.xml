<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wbiao.mapper.AddressMapper">
   <select id="selectAddressByUsername" resultType="Address">
       select * from t_address where username = #{username}
   </select>

    <insert id="addAddress">
        insert into t_address(username,name,phone,province,city,region,detail_address,send_status)
            values (#{username},#{name},#{phone},#{province},#{city},#{region},#{detail_address},#{send_status})
    </insert>
    <select id="selectAddressByNameAndStatus" resultType="Address">
        select * from t_address where username = #{username} and send_status = 1
    </select>
    <update id="updateStatus">
        update t_address set send_status = 0 where id = #{id}
    </update>

    <update id="updateAddressById">
        update t_address set username = #{username},name = #{name},phone = #{phone},province = #{province},city = #{city},region = #{region},detail_address = #{detail_address},send_status = #{send_status}
         where id = #{id}
    </update>
</mapper>