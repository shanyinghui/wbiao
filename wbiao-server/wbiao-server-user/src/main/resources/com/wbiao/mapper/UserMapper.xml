<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wbiao.mapper.UserMapper">
   <select id="selectUserByUsername" resultType="User">
       select * from t_user where username = #{username}
   </select>
    <insert id="addUser">
        insert into t_user (username,password,phone,created,updated,state)
            values(#{username},#{password},#{phone},#{created},#{updated},#{state})
    </insert>
    <select id="selectUserByPhone" resultType="User">
        select * from t_user where phone = #{phone}
    </select>
    <select id="selectUserByUid" resultType="User">
        select * from t_user where uid = #{uid}
    </select>
    <insert id="addUserByUid">
        insert into t_user (username,nick_name,head_pic,sex,created,updated,state,uid,login_type)
            values(#{username},#{nick_name},#{head_pic},#{sex},#{created},#{updated},#{state},#{uid},#{login_type})
    </insert>
</mapper>